# Requirement

- Doctor can active/inactive schedule meeting
- Doctor can config schedule
- Doctor can active/inactive instant meeting

- Doctor can booked by Patient

- Pool is like a virtual worksplace
- Pool usecase we have pool `a`, `b`, `c` for tenant `X` can book doctor in pool `a`, but tenant `Y` can book all
- Doctor can available in single pool
